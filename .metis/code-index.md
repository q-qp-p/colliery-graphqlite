# Code Index

> Generated: 2026-03-17T13:02:09Z | 54 files | JavaScript, Python, Rust

## Project Structure

```
├── bindings/
│   ├── python/
│   │   ├── src/
│   │   │   └── graphqlite/
│   │   │       ├── __init__.py
│   │   │       ├── _platform.py
│   │   │       ├── algorithms/
│   │   │       │   ├── __init__.py
│   │   │       │   ├── _parsing.py
│   │   │       │   ├── centrality.py
│   │   │       │   ├── community.py
│   │   │       │   ├── components.py
│   │   │       │   ├── export.py
│   │   │       │   ├── paths.py
│   │   │       │   ├── similarity.py
│   │   │       │   └── traversal.py
│   │   │       ├── connection.py
│   │   │       ├── graph/
│   │   │       │   ├── __init__.py
│   │   │       │   ├── _base.py
│   │   │       │   ├── batch.py
│   │   │       │   ├── bulk.py
│   │   │       │   ├── edges.py
│   │   │       │   ├── nodes.py
│   │   │       │   └── queries.py
│   │   │       ├── manager.py
│   │   │       └── utils.py
│   │   └── tests/
│   │       ├── test_connection.py
│   │       ├── test_graph.py
│   │       └── test_manager.py
│   └── rust/
│       ├── build.rs
│       ├── src/
│       │   ├── algorithms/
│       │   │   ├── centrality.rs
│       │   │   ├── community.rs
│       │   │   ├── components.rs
│       │   │   ├── mod.rs
│       │   │   ├── parsing.rs
│       │   │   ├── paths.rs
│       │   │   ├── similarity.rs
│       │   │   └── traversal.rs
│       │   ├── connection.rs
│       │   ├── error.rs
│       │   ├── graph/
│       │   │   ├── batch.rs
│       │   │   ├── bulk.rs
│       │   │   ├── edges.rs
│       │   │   ├── mod.rs
│       │   │   ├── nodes.rs
│       │   │   └── queries.rs
│       │   ├── lib.rs
│       │   ├── manager.rs
│       │   ├── platform.rs
│       │   ├── query_builder.rs
│       │   ├── result.rs
│       │   └── utils.rs
│       └── tests/
│           └── integration.rs
├── docs/
│   └── theme/
│       └── version-select.js
└── examples/
    └── llm-graphrag/
        ├── analyze.py
        ├── hotpotqa.py
        ├── ingest.py
        ├── ollama_client.py
        └── rag.py
```

## Modules

### bindings/python/src/graphqlite

> *Semantic summary to be generated by AI agent.*

#### bindings/python/src/graphqlite/__init__.py

- pub `loadable_path` function L20-37 — `def loadable_path() -> str` — Return the path to the loadable GraphQLite extension.
- pub `load` function L40-76 — `def load(conn, entry_point: Optional[str] = None) -> None` — Load the GraphQLite extension into an existing SQLite connection.

#### bindings/python/src/graphqlite/_platform.py

- pub `get_extension_name` function L9-20 — `def get_extension_name() -> str` — Get the platform-specific extension filename.
- pub `get_extension_search_paths` function L23-43 — `def get_extension_search_paths() -> list[Path]` — Get ordered list of paths to search for the extension.
- pub `find_extension` function L46-74 — `def find_extension(extension_path: Optional[str] = None) -> str` — Find the GraphQLite extension library.
- pub `get_loadable_path` function L77-89 — `def get_loadable_path(extension_path: Optional[str] = None) -> str` — Get extension path in SQLite-loadable format (without file extension).

#### bindings/python/src/graphqlite/connection.py

- pub `CypherResult` class L11-34 — `{ __init__, columns, to_list }` — Result from a Cypher query, iterable as rows.
- pub `__init__` method L14-16 — `def __init__(self, data: list[dict[str, Any]], columns: list[str])`
- pub `__iter__` method L18-19 — `def __iter__(self) -> Iterator[dict[str, Any]]`
- pub `__len__` method L21-22 — `def __len__(self) -> int`
- pub `__getitem__` method L24-25 — `def __getitem__(self, index: int) -> dict[str, Any]`
- pub `to_list` method L32-34 — `def to_list(self) -> list[dict[str, Any]]` — Return results as a list of dictionaries.
- pub `Connection` class L37-207 — `{ __init__, cypher, execute, commit, rollback, close, sqlite_connection }` — GraphQLite database connection with Cypher query support.
- pub `__init__` method L40-49 — `def __init__(self, conn: sqlite3.Connection, extension_path: Optional[str] = Non...` — Initialize GraphQLite connection.
- pub `cypher` method L128-180 — `def cypher(self, query: str, params: Optional[dict[str, Any]] = None) -> CypherR...` — Execute a Cypher query with optional parameters.
- pub `execute` method L182-184 — `def execute(self, sql: str, parameters: tuple = ()) -> sqlite3.Cursor` — Execute a raw SQL query.
- pub `commit` method L186-188 — `def commit(self) -> None` — Commit the current transaction.
- pub `rollback` method L190-192 — `def rollback(self) -> None` — Rollback the current transaction.
- pub `close` method L194-196 — `def close(self) -> None` — Close the database connection.
- pub `__enter__` method L198-199 — `def __enter__(self) -> "Connection"`
- pub `__exit__` method L201-202 — `def __exit__(self, exc_type, exc_val, exc_tb) -> None`
- pub `connect` function L210-234 — `def connect( database: Union[str, Path] = ":memory:", extension_path: Optional[s...` — Open a GraphQLite database connection.
- pub `wrap` function L237-254 — `def wrap(conn: sqlite3.Connection, extension_path: Optional[str] = None) -> Conn...` — Wrap an existing SQLite connection with GraphQLite support.
-  `_find_extension` method L51-87 — `def _find_extension(self) -> str` — Find the GraphQLite extension library.
-  `_load_extension` method L89-126 — `def _load_extension(self, extension_path: Optional[str] = None) -> None` — Load the GraphQLite SQLite extension.

#### bindings/python/src/graphqlite/manager.py

- pub `GraphManager` class L15-365 — `{ __init__, list, exists, create, open, open_or_create, drop, query, query_sql, ...` — Manager for multiple graph databases in a directory.
- pub `__init__` method L32-50 — `def __init__( self, base_path: Union[str, Path], extension_path: Optional[str] =...` — Initialize GraphManager.
- pub `list` method L68-82 — `def list(self) -> list[str]` — List all available graphs in the base directory.
- pub `exists` method L84-94 — `def exists(self, name: str) -> bool` — Check if a graph exists.
- pub `create` method L96-119 — `def create(self, name: str) -> Graph` — Create a new graph.
- pub `open` method L121-151 — `def open(self, name: str) -> Graph` — Open an existing graph.
- pub `open_or_create` method L153-168 — `def open_or_create(self, name: str) -> Graph` — Open a graph, creating it if it doesn't exist.
- pub `drop` method L170-203 — `def drop(self, name: str) -> None` — Delete a graph and its database file.
- pub `query` method L205-290 — `def query( self, cypher: str, graphs: Optional[list[str]] = None, params: Option...` — Execute a cross-graph Cypher query.
- pub `query_sql` method L292-335 — `def query_sql( self, sql: str, graphs: list[str], parameters: tuple = () ) -> li...` — Execute a raw SQL query across attached graphs.
- pub `close` method L337-345 — `def close(self) -> None` — Close all open graph connections and the coordinator.
- pub `__enter__` method L347-349 — `def __enter__(self) -> "GraphManager"` — Context manager entry.
- pub `__exit__` method L351-353 — `def __exit__(self, exc_type, exc_val, exc_tb) -> None` — Context manager exit - close all connections.
- pub `__iter__` method L355-357 — `def __iter__(self) -> Iterator[str]` — Iterate over graph names.
- pub `__contains__` method L359-361 — `def __contains__(self, name: str) -> bool` — Check if a graph exists.
- pub `__len__` method L363-365 — `def __len__(self) -> int` — Return number of graphs.
- pub `graphs` function L368-391 — `def graphs( base_path: Union[str, Path], extension_path: Optional[str] = None ) ...` — Create a GraphManager for managing multiple graphs.
-  `_graph_path` method L52-54 — `def _graph_path(self, name: str) -> Path` — Get the file path for a graph.
-  `_ensure_coordinator` method L56-66 — `def _ensure_coordinator(self) -> sqlite3.Connection` — Get or create the coordinator connection for cross-graph queries.

#### bindings/python/src/graphqlite/utils.py

- pub `escape_string` function L24-41 — `def escape_string(s: str) -> str` — Escape a string for use in Cypher queries.
- pub `sanitize_rel_type` function L44-61 — `def sanitize_rel_type(rel_type: str) -> str` — Sanitize a relationship type for use in Cypher.
- pub `format_props` function L64-85 — `def format_props(props: dict[str, Any], escape_fn=escape_string) -> str` — Format a properties dict as a Cypher property string.

### bindings/python/src/graphqlite/algorithms

> *Semantic summary to be generated by AI agent.*

#### bindings/python/src/graphqlite/algorithms/_parsing.py

- pub `extract_algo_array` function L14-36 — `def extract_algo_array(result: List[dict]) -> List[dict]` — Extract wrapped array results from graph algorithms.
- pub `parse_score_result` function L39-52 — `def parse_score_result(row: dict, score_key: str = "score") -> Optional[dict]` — Parse a result row with node_id, user_id, and a score field.
- pub `parse_community_result` function L55-68 — `def parse_community_result(row: dict) -> Optional[dict]` — Parse a community detection result row.
- pub `parse_component_result` function L71-84 — `def parse_component_result(row: dict) -> Optional[dict]` — Parse a connected components result row.
- pub `parse_traversal_result` function L87-100 — `def parse_traversal_result(row: dict) -> Optional[dict]` — Parse a BFS/DFS traversal result row.
- pub `safe_float` function L103-110 — `def safe_float(val: Any, default: float = 0.0) -> float` — Safely convert a value to float.
- pub `safe_int` function L113-120 — `def safe_int(val: Any, default: int = 0) -> int` — Safely convert a value to int.

#### bindings/python/src/graphqlite/algorithms/centrality.py

- pub `CentralityMixin` class L9-178 — `(BaseMixin) { pagerank, degree_centrality, betweenness_centrality, closeness_cen...` — Mixin providing centrality algorithm methods.
- pub `pagerank` method L12-45 — `def pagerank( self, damping: float = 0.85, iterations: int = 20 ) -> list[dict]` — Run PageRank algorithm.
- pub `degree_centrality` method L47-77 — `def degree_centrality(self) -> list[dict]` — Calculate degree centrality for all nodes.
- pub `betweenness_centrality` method L79-106 — `def betweenness_centrality(self) -> list[dict]` — Calculate betweenness centrality for all nodes.
- pub `closeness_centrality` method L111-139 — `def closeness_centrality(self) -> list[dict]` — Calculate closeness centrality for all nodes.
- pub `eigenvector_centrality` method L144-178 — `def eigenvector_centrality(self, iterations: int = 100) -> list[dict]` — Calculate eigenvector centrality for all nodes.

#### bindings/python/src/graphqlite/algorithms/community.py

- pub `CommunityMixin` class L9-133 — `(BaseMixin) { community_detection, louvain, leiden_communities }` — Mixin providing community detection algorithm methods.
- pub `community_detection` method L12-37 — `def community_detection(self, iterations: int = 10) -> list[dict]` — Run community detection using label propagation.
- pub `louvain` method L39-69 — `def louvain(self, resolution: float = 1.0) -> list[dict]` — Run Louvain community detection algorithm.
- pub `leiden_communities` method L71-133 — `def leiden_communities( self, resolution: float = 1.0, random_seed: Optional[int...` — Run Leiden community detection.

#### bindings/python/src/graphqlite/algorithms/components.py

- pub `ComponentsMixin` class L7-74 — `(BaseMixin) { weakly_connected_components, strongly_connected_components }` — Mixin providing connected components algorithm methods.
- pub `weakly_connected_components` method L10-37 — `def weakly_connected_components(self) -> list[dict]` — Find weakly connected components in the graph.
- pub `strongly_connected_components` method L43-71 — `def strongly_connected_components(self) -> list[dict]` — Find strongly connected components in the graph.

#### bindings/python/src/graphqlite/algorithms/export.py

- pub `ExportMixin` class L6-55 — `(BaseMixin) { to_rustworkx }` — Mixin providing graph export functionality.
- pub `to_rustworkx` method L9-55 — `def to_rustworkx(self)` — Export the graph to a rustworkx PyDiGraph.

#### bindings/python/src/graphqlite/algorithms/paths.py

- pub `PathsMixin` class L10-141 — `(BaseMixin) { shortest_path, astar, all_pairs_shortest_path }` — Mixin providing path finding algorithm methods.
- pub `shortest_path` method L13-62 — `def shortest_path( self, source_id: str, target_id: str, weight_property: Option...` — Find the shortest path between two nodes using Dijkstra's algorithm.
- pub `astar` method L67-106 — `def astar( self, source_id: str, target_id: str, lat_prop: str | None = None, lo...` — Find shortest path using A* algorithm with heuristic guidance.
- pub `all_pairs_shortest_path` method L111-138 — `def all_pairs_shortest_path(self) -> list[dict]` — Compute shortest paths between all pairs of nodes.

#### bindings/python/src/graphqlite/algorithms/similarity.py

- pub `SimilarityMixin` class L8-122 — `(BaseMixin) { node_similarity, knn, triangle_count }` — Mixin providing similarity and clustering algorithm methods.
- pub `node_similarity` method L11-57 — `def node_similarity( self, node1_id: str | None = None, node2_id: str | None = N...` — Compute node similarity using Jaccard coefficient.
- pub `knn` method L59-90 — `def knn(self, node_id: str, k: int = 10) -> list[dict]` — Find K-nearest neighbors using Jaccard similarity.
- pub `triangle_count` method L92-119 — `def triangle_count(self) -> list[dict]` — Count triangles each node participates in.

#### bindings/python/src/graphqlite/algorithms/traversal.py

- pub `TraversalMixin` class L7-83 — `(BaseMixin) { bfs, dfs }` — Mixin providing graph traversal algorithm methods.
- pub `bfs` method L10-43 — `def bfs( self, start_id: str, max_depth: int = -1 ) -> list[dict]` — Perform breadth-first search traversal from a starting node.
- pub `dfs` method L48-80 — `def dfs( self, start_id: str, max_depth: int = -1 ) -> list[dict]` — Perform depth-first search traversal from a starting node.

### bindings/python/src/graphqlite/graph

> *Semantic summary to be generated by AI agent.*

#### bindings/python/src/graphqlite/graph/__init__.py

- pub `Graph` class L34-196 — `( NodesMixin, EdgesMixin, QueriesMixin, BatchMixin, BulkMixin, CentralityMixin, ...` — High-level graph interface for GraphQLite.
- pub `__init__` method L65-81 — `def __init__( self, db_path: Union[str, Path] = ":memory:", namespace: str = "de...` — Initialize a Graph instance.
- pub `close` method L88-90 — `def close(self) -> None` — Close the database connection.
- pub `load_graph` method L93-116 — `def load_graph(self) -> dict` — Load the graph into an in-memory CSR cache for fast algorithm execution.
- pub `unload_graph` method L118-137 — `def unload_graph(self) -> dict` — Free the cached graph from memory.
- pub `reload_graph` method L139-158 — `def reload_graph(self) -> dict` — Reload the graph cache with the latest data.
- pub `graph_loaded` method L169-187 — `def graph_loaded(self) -> bool` — Check if the graph cache is currently loaded.
- pub `__enter__` method L189-191 — `def __enter__(self)` — Context manager entry.
- pub `__exit__` method L193-196 — `def __exit__(self, exc_type, exc_val, exc_tb)` — Context manager exit - close connection.
- pub `graph` function L199-221 — `def graph( db_path: Union[str, Path] = ":memory:", namespace: str = "default", e...` — Create a new Graph instance.

#### bindings/python/src/graphqlite/graph/_base.py

- pub `BaseMixin` class L8-21 — `-` — Base mixin providing connection access and utility methods.
-  `_escape` method L15-17 — `def _escape(self, s: str) -> str` — Escape a string for Cypher queries.
-  `_format_props` method L19-21 — `def _format_props(self, props: dict[str, Any]) -> str` — Format a properties dict as a Cypher property string.

#### bindings/python/src/graphqlite/graph/batch.py

- pub `BatchMixin` class L12-55 — `(BaseMixin) { upsert_nodes_batch, upsert_edges_batch }` — Mixin providing batch operations.
- pub `upsert_nodes_batch` method L15-34 — `def upsert_nodes_batch( self, nodes: list[tuple[str, dict[str, Any], str]] ) -> ...` — Batch upsert multiple nodes.
- pub `upsert_edges_batch` method L36-55 — `def upsert_edges_batch( self, edges: list[tuple[str, str, dict[str, Any], str]] ...` — Batch upsert multiple edges.

#### bindings/python/src/graphqlite/graph/bulk.py

- pub `BulkInsertResult` class L30-40 — `-` — Result of a bulk insert operation.
- pub `BulkMixin` class L43-396 — `(BaseMixin) { insert_nodes_bulk, insert_edges_bulk, insert_graph_bulk, resolve_n...` — Mixin providing bulk insert operations.
- pub `insert_nodes_bulk` method L46-126 — `def insert_nodes_bulk( self, nodes: list[tuple[str, dict[str, Any], str]], ) -> ...` — Insert multiple nodes in a single transaction with minimal overhead.
- pub `insert_edges_bulk` method L128-227 — `def insert_edges_bulk( self, edges: list[tuple[str, str, dict[str, Any], str]], ...` — Insert multiple edges using pre-resolved internal IDs.
- pub `insert_graph_bulk` method L229-267 — `def insert_graph_bulk( self, nodes: list[tuple[str, dict[str, Any], str]], edges...` — Bulk insert both nodes and edges in a single operation.
- pub `resolve_node_ids` method L269-319 — `def resolve_node_ids( self, external_ids: list[str], ) -> dict[str, int]` — Resolve multiple external node IDs to internal rowids.
-  `_ensure_property_key` method L323-333 — `def _ensure_property_key(self, conn, key: str) -> int` — Ensure a property key exists and return its ID.
-  `_lookup_node_id` method L335-354 — `def _lookup_node_id(self, conn, external_id: str) -> int` — Look up a node's internal ID by external ID.
-  `_insert_property` method L356-381 — `def _insert_property(self, conn, entity_type: str, entity_id: int, key_id: int, ...` — Insert a property value into the appropriate typed table.
-  `_sanitize_rel_type` method L383-396 — `def _sanitize_rel_type(self, rel_type: str) -> str` — Sanitize a relationship type for use in the database.

#### bindings/python/src/graphqlite/graph/edges.py

- pub `EdgesMixin` class L9-128 — `(BaseMixin) { has_edge, get_edge, upsert_edge, delete_edge, get_all_edges }` — Mixin providing edge CRUD operations.
- pub `has_edge` method L12-33 — `def has_edge(self, source_id: str, target_id: str, rel_type: Optional[str] = Non...` — Check if an edge exists between two nodes.
- pub `get_edge` method L35-54 — `def get_edge(self, source_id: str, target_id: str, rel_type: Optional[str] = Non...` — Get edge properties between two nodes.
- pub `upsert_edge` method L56-101 — `def upsert_edge( self, source_id: str, target_id: str, edge_data: dict[str, Any]...` — Create or update an edge between two nodes.
- pub `delete_edge` method L103-116 — `def delete_edge(self, source_id: str, target_id: str, rel_type: Optional[str] = ...` — Delete edge between two nodes.
- pub `get_all_edges` method L118-128 — `def get_all_edges(self) -> list[dict]` — Get all edges with source and target info.

#### bindings/python/src/graphqlite/graph/nodes.py

- pub `NodesMixin` class L9-124 — `(BaseMixin) { has_node, get_node, upsert_node, delete_node, get_all_nodes }` — Mixin providing node CRUD operations.
- pub `has_node` method L12-28 — `def has_node(self, node_id: str) -> bool` — Check if a node exists.
- pub `get_node` method L30-45 — `def get_node(self, node_id: str) -> Optional[dict]` — Get a node by ID.
- pub `upsert_node` method L47-84 — `def upsert_node( self, node_id: str, node_data: dict[str, Any], label: str = "En...` — Create or update a node.
- pub `delete_node` method L86-95 — `def delete_node(self, node_id: str) -> None` — Delete a node and its relationships.
- pub `get_all_nodes` method L97-124 — `def get_all_nodes(self, label: Optional[str] = None) -> list[dict]` — Get all nodes, optionally filtered by label.

#### bindings/python/src/graphqlite/graph/queries.py

- pub `QueriesMixin` class L8-147 — `(BaseMixin) { node_degree, get_neighbors, get_node_edges, get_edges_from, get_ed...` — Mixin providing graph query operations.
- pub `node_degree` method L11-30 — `def node_degree(self, node_id: str) -> int` — Get the degree (number of connections) of a node.
- pub `get_neighbors` method L32-48 — `def get_neighbors(self, node_id: str) -> list[dict]` — Get all neighboring nodes.
- pub `get_node_edges` method L50-64 — `def get_node_edges(self, node_id: str) -> list[tuple[str, str, dict]]` — Get all edges connected to a node.
- pub `get_edges_from` method L66-80 — `def get_edges_from(self, node_id: str) -> list[dict]` — Get all outgoing edges from a node.
- pub `get_edges_to` method L82-96 — `def get_edges_to(self, node_id: str) -> list[dict]` — Get all incoming edges to a node.
- pub `get_edges_by_type` method L98-115 — `def get_edges_by_type(self, node_id: str, rel_type: str) -> list[dict]` — Get outgoing edges of a specific type from a node.
- pub `stats` method L117-133 — `def stats(self) -> dict[str, int]` — Get graph statistics.
- pub `query` method L135-147 — `def query(self, cypher: str, params: Optional[dict] = None) -> list[dict]` — Execute a raw Cypher query with optional parameters.

### bindings/python/tests

> *Semantic summary to be generated by AI agent.*

#### bindings/python/tests/test_connection.py

- pub `get_extension_path` function L13-27 — `def get_extension_path()` — Get path to the built extension.
- pub `test_connect_memory` function L43-47 — `def test_connect_memory()`
- pub `test_connect_file` function L50-56 — `def test_connect_file(tmp_path)`
- pub `test_connect_context_manager` function L59-63 — `def test_connect_context_manager()`
- pub `test_wrap_existing_connection` function L66-72 — `def test_wrap_existing_connection()`
- pub `test_sqlite_connection_property` function L75-77 — `def test_sqlite_connection_property(db)`
- pub `test_create_node` function L84-89 — `def test_create_node(db)`
- pub `test_create_relationship` function L92-102 — `def test_create_relationship(db)`
- pub `test_return_scalar` function L105-109 — `def test_return_scalar(db)`
- pub `test_return_multiple_rows` function L112-118 — `def test_return_multiple_rows(db)`
- pub `test_aggregation` function L121-127 — `def test_aggregation(db)`
- pub `test_empty_result` function L130-132 — `def test_empty_result(db)`
- pub `test_result_iteration` function L139-144 — `def test_result_iteration(db)`
- pub `test_result_indexing` function L147-152 — `def test_result_indexing(db)`
- pub `test_result_columns` function L155-157 — `def test_result_columns(db)`
- pub `test_result_to_list` function L160-165 — `def test_result_to_list(db)`
- pub `test_result_len` function L168-172 — `def test_result_len(db)`
- pub `test_pagerank_function` function L179-188 — `def test_pagerank_function(db)`
- pub `test_label_propagation_function` function L191-199 — `def test_label_propagation_function(db)`
- pub `test_version_exists` function L206-208 — `def test_version_exists()`
- pub `test_exports` function L211-216 — `def test_exports()`
- pub `test_parameterized_string` function L223-234 — `def test_parameterized_string(db)` — Test parameterized query with string parameter.
- pub `test_parameterized_integer` function L237-249 — `def test_parameterized_integer(db)` — Test parameterized query with integer parameter.
- pub `test_parameterized_multiple` function L252-264 — `def test_parameterized_multiple(db)` — Test parameterized query with multiple parameters.
- pub `test_parameterized_no_match` function L267-279 — `def test_parameterized_no_match(db)` — Test parameterized query that matches nothing.
- pub `test_parameterized_sql_injection_safe` function L282-302 — `def test_parameterized_sql_injection_safe(db)` — Test that parameters prevent SQL injection.
- pub `test_remove_node_property` function L309-328 — `def test_remove_node_property(db)` — Test REMOVE clause for removing a node property.
- pub `test_remove_multiple_properties` function L331-344 — `def test_remove_multiple_properties(db)` — Test REMOVE clause for removing multiple properties at once.
- pub `test_remove_label` function L347-367 — `def test_remove_label(db)` — Test REMOVE clause for removing a label from a node.
- pub `test_remove_edge_property` function L370-392 — `def test_remove_edge_property(db)` — Test REMOVE clause for removing an edge property.
- pub `test_remove_with_where` function L395-420 — `def test_remove_with_where(db)` — Test REMOVE clause with WHERE filtering.
- pub `test_remove_nonexistent_property` function L423-433 — `def test_remove_nonexistent_property(db)` — Test REMOVE clause on a non-existent property (should not error).
- pub `test_remove_no_match` function L436-440 — `def test_remove_no_match(db)` — Test REMOVE clause when no nodes match.
- pub `test_in_literal_list_match` function L447-452 — `def test_in_literal_list_match(db)` — Test IN operator with literal list - matching value.
- pub `test_in_literal_list_no_match` function L455-460 — `def test_in_literal_list_no_match(db)` — Test IN operator with literal list - non-matching value.
- pub `test_in_with_where_clause` function L463-474 — `def test_in_with_where_clause(db)` — Test IN operator in WHERE clause for filtering.
- pub `test_in_with_integers` function L477-488 — `def test_in_with_integers(db)` — Test IN operator with integer values.
- pub `test_in_empty_result` function L491-498 — `def test_in_empty_result(db)` — Test IN operator when no values match.
- pub `test_starts_with_match` function L502-506 — `def test_starts_with_match(db)` — Test STARTS WITH operator - matching prefix.
- pub `test_starts_with_no_match` function L509-513 — `def test_starts_with_no_match(db)` — Test STARTS WITH operator - non-matching prefix.
- pub `test_ends_with_match` function L516-520 — `def test_ends_with_match(db)` — Test ENDS WITH operator - matching suffix.
- pub `test_ends_with_no_match` function L523-527 — `def test_ends_with_no_match(db)` — Test ENDS WITH operator - non-matching suffix.
- pub `test_contains_match` function L530-534 — `def test_contains_match(db)` — Test CONTAINS operator - substring exists.
- pub `test_contains_no_match` function L537-541 — `def test_contains_no_match(db)` — Test CONTAINS operator - substring does not exist.
- pub `test_string_operators_in_where` function L544-572 — `def test_string_operators_in_where(db)` — Test string operators in WHERE clause.
- pub `test_string_operators_with_property` function L575-602 — `def test_string_operators_with_property(db)` — Test string operators comparing property to literal.
- pub `test_union_basic` function L609-614 — `def test_union_basic(db)` — Test basic UNION query.
- pub `test_union_all` function L617-625 — `def test_union_all(db)` — Test UNION ALL (keeps duplicates).
- pub `test_union_distinct` function L628-635 — `def test_union_distinct(db)` — Test UNION removes duplicates.
- pub `test_union_with_match` function L638-651 — `def test_union_with_match(db)` — Test UNION with MATCH queries.
- pub `test_toupper_function` function L658-662 — `def test_toupper_function(db)` — Test toUpper() function.
- pub `test_tolower_function` function L665-669 — `def test_tolower_function(db)` — Test toLower() function.
- pub `test_trim_function` function L672-676 — `def test_trim_function(db)` — Test trim() function.
- pub `test_substring_function` function L679-683 — `def test_substring_function(db)` — Test substring() function.
- pub `test_replace_function` function L686-690 — `def test_replace_function(db)` — Test replace() function.
- pub `test_reverse_function` function L693-697 — `def test_reverse_function(db)` — Test reverse() function.
- pub `test_left_right_functions` function L700-705 — `def test_left_right_functions(db)` — Test left() and right() functions.
- pub `test_abs_function` function L712-716 — `def test_abs_function(db)` — Test abs() function.
- pub `test_ceil_floor_functions` function L719-724 — `def test_ceil_floor_functions(db)` — Test ceil() and floor() functions.
- pub `test_round_function` function L727-731 — `def test_round_function(db)` — Test round() function.
- pub `test_sqrt_function` function L734-738 — `def test_sqrt_function(db)` — Test sqrt() function.
- pub `test_sign_function` function L741-747 — `def test_sign_function(db)` — Test sign() function.
- pub `test_size_on_list` function L754-758 — `def test_size_on_list(db)` — Test size() function on list.
- pub `test_head_function` function L761-765 — `def test_head_function(db)` — Test head() function.
- pub `test_tail_function` function L768-777 — `def test_tail_function(db)` — Test tail() function.
- pub `test_last_function` function L780-784 — `def test_last_function(db)` — Test last() function.
- pub `test_range_function` function L787-796 — `def test_range_function(db)` — Test range() function.
- pub `test_with_simple` function L803-813 — `def test_with_simple(db)` — Test simple WITH clause.
- pub `test_with_where` function L816-827 — `def test_with_where(db)` — Test WITH clause with WHERE filter.
- pub `test_with_aggregation` function L830-844 — `def test_with_aggregation(db)` — Test WITH clause with aggregation.
- pub `test_case_simple` function L851-863 — `def test_case_simple(db)` — Test simple CASE expression (using searched CASE syntax).
- pub `test_case_with_else` function L866-877 — `def test_case_with_else(db)` — Test CASE expression with ELSE.
- pub `test_case_searched` function L880-890 — `def test_case_searched(db)` — Test searched CASE expression.
- pub `test_case_with_property` function L893-910 — `def test_case_with_property(db)` — Test CASE with property access.
- pub `test_coalesce_basic` function L917-921 — `def test_coalesce_basic(db)` — Test coalesce() returns first non-null.
- pub `test_coalesce_with_property` function L924-932 — `def test_coalesce_with_property(db)` — Test coalesce() with property that may be null.
- pub `test_is_null` function L939-947 — `def test_is_null(db)` — Test IS NULL predicate.
- pub `test_is_not_null` function L950-958 — `def test_is_not_null(db)` — Test IS NOT NULL predicate.
- pub `test_tostring_function` function L965-969 — `def test_tostring_function(db)` — Test toString() function.
- pub `test_tointeger_function` function L972-976 — `def test_tointeger_function(db)` — Test toInteger() function.
- pub `test_tofloat_function` function L979-983 — `def test_tofloat_function(db)` — Test toFloat() function.
- pub `test_foreach_create_nodes` function L990-1001 — `def test_foreach_create_nodes(db)` — Test FOREACH creating multiple nodes.
- pub `test_foreach_set_property` function L1004-1016 — `def test_foreach_set_property(db)` — Test FOREACH setting properties on nodes.
- pub `test_varlen_any_hops` function L1023-1033 — `def test_varlen_any_hops(db)` — Test variable-length relationship with any number of hops.
- pub `test_varlen_exact_hops` function L1036-1044 — `def test_varlen_exact_hops(db)` — Test variable-length relationship with exact hop count.
- pub `test_varlen_bounded` function L1047-1059 — `def test_varlen_bounded(db)` — Test variable-length relationship with min/max bounds.
- pub `test_path_length` function L1066-1074 — `def test_path_length(db)` — Test length() function on simple path (not varlen).
- pub `test_path_nodes` function L1077-1091 — `def test_path_nodes(db)` — Test nodes() function on simple path.
- pub `test_path_relationships` function L1094-1108 — `def test_path_relationships(db)` — Test relationships() function on simple path.
- pub `test_exists_property` function L1115-1125 — `def test_exists_property(db)` — Test EXISTS on property.
- pub `test_not_exists_property` function L1128-1138 — `def test_not_exists_property(db)` — Test NOT EXISTS on property.
- pub `test_exists_pattern` function L1141-1151 — `def test_exists_pattern(db)` — Test EXISTS on pattern.
- pub `test_multiple_labels_create` function L1158-1163 — `def test_multiple_labels_create(db)` — Test creating node with multiple labels.
- pub `test_multiple_labels_match_any` function L1166-1175 — `def test_multiple_labels_match_any(db)` — Test matching nodes with multiple labels.
- pub `test_labels_function` function L1178-1191 — `def test_labels_function(db)` — Test labels() function.
- pub `test_return_node_all_labels` function L1194-1206 — `def test_return_node_all_labels(db)` — Test that RETURN n includes all labels, not just the first (issue #21).
- pub `test_optional_match_found` function L1213-1223 — `def test_optional_match_found(db)` — Test OPTIONAL MATCH when pattern exists.
- pub `test_optional_match_not_found` function L1226-1236 — `def test_optional_match_not_found(db)` — Test OPTIONAL MATCH when pattern doesn't exist.
- pub `test_optional_match_partial` function L1239-1253 — `def test_optional_match_partial(db)` — Test OPTIONAL MATCH with some nodes having matches.
- pub `test_unwind_list` function L1260-1269 — `def test_unwind_list(db)` — Test UNWIND with list literal.
- pub `test_unwind_with_create` function L1272-1282 — `def test_unwind_with_create(db)` — Test UNWIND to create multiple nodes.
- pub `test_unwind_empty_list` function L1285-1291 — `def test_unwind_empty_list(db)` — Test UNWIND with empty list returns no rows.
- pub `test_unwind_with_index` function L1294-1305 — `def test_unwind_with_index(db)` — Test array subscript syntax.

#### bindings/python/tests/test_graph.py

- pub `get_extension_path` function L12-26 — `def get_extension_path()` — Get path to the built extension.
- pub `test_escape_string_single_quotes` function L42-43 — `def test_escape_string_single_quotes()`
- pub `test_escape_string_double_quotes` function L46-47 — `def test_escape_string_double_quotes()`
- pub `test_escape_string_backslash` function L50-51 — `def test_escape_string_backslash()`
- pub `test_escape_string_newlines` function L54-55 — `def test_escape_string_newlines()`
- pub `test_escape_string_tabs` function L58-59 — `def test_escape_string_tabs()`
- pub `test_sanitize_rel_type_passthrough` function L62-63 — `def test_sanitize_rel_type_passthrough()`
- pub `test_sanitize_rel_type_special_chars` function L66-67 — `def test_sanitize_rel_type_special_chars()`
- pub `test_sanitize_rel_type_leading_digit` function L70-72 — `def test_sanitize_rel_type_leading_digit()`
- pub `test_sanitize_rel_type_reserved_word` function L75-77 — `def test_sanitize_rel_type_reserved_word()`
- pub `test_cypher_reserved_contains_keywords` function L80-83 — `def test_cypher_reserved_contains_keywords()`
- pub `test_graph_create` function L90-94 — `def test_graph_create()`
- pub `test_graph_context_manager` function L97-101 — `def test_graph_context_manager()`
- pub `test_graph_factory_function` function L104-108 — `def test_graph_factory_function()`
- pub `test_graph_connection_property` function L111-113 — `def test_graph_connection_property(g)`
- pub `test_graph_namespace` function L116-117 — `def test_graph_namespace(g)`
- pub `test_has_node_false` function L124-125 — `def test_has_node_false(g)`
- pub `test_upsert_node_creates` function L128-130 — `def test_upsert_node_creates(g)`
- pub `test_get_node` function L133-136 — `def test_get_node(g)`
- pub `test_get_node_nonexistent` function L139-140 — `def test_get_node_nonexistent(g)`
- pub `test_upsert_node_updates` function L143-146 — `def test_upsert_node_updates(g)`
- pub `test_delete_node` function L149-153 — `def test_delete_node(g)`
- pub `test_get_all_nodes` function L156-160 — `def test_get_all_nodes(g)`
- pub `test_get_all_nodes_by_label` function L163-167 — `def test_get_all_nodes_by_label(g)`
- pub `test_has_edge_false` function L174-177 — `def test_has_edge_false(g)`
- pub `test_upsert_edge_creates` function L180-184 — `def test_upsert_edge_creates(g)`
- pub `test_get_edge` function L187-191 — `def test_get_edge(g)`
- pub `test_delete_edge` function L194-200 — `def test_delete_edge(g)`
- pub `test_get_all_edges` function L203-209 — `def test_get_all_edges(g)`
- pub `test_upsert_edge_multiple_types` function L212-221 — `def test_upsert_edge_multiple_types(g)`
- pub `test_upsert_edge_updates_properties` function L224-239 — `def test_upsert_edge_updates_properties(g)`
- pub `test_upsert_edge_update_empty_props` function L242-252 — `def test_upsert_edge_update_empty_props(g)`
- pub `test_get_edge_by_type` function L255-271 — `def test_get_edge_by_type(g)` — get_edge should be able to retrieve a specific edge type.
- pub `test_delete_edge_by_type` function L274-287 — `def test_delete_edge_by_type(g)` — delete_edge should only remove the specified edge type.
- pub `test_has_edge_by_type` function L290-298 — `def test_has_edge_by_type(g)` — has_edge should be able to check for a specific edge type.
- pub `test_node_degree` function L305-313 — `def test_node_degree(g)`
- pub `test_get_neighbors` function L316-322 — `def test_get_neighbors(g)`
- pub `test_stats` function L325-333 — `def test_stats(g)`
- pub `test_query_raw_cypher` function L336-341 — `def test_query_raw_cypher(g)`
- pub `test_node_with_single_quotes` function L348-350 — `def test_node_with_single_quotes(g)`
- pub `test_node_with_double_quotes` function L353-355 — `def test_node_with_double_quotes(g)`
- pub `test_node_with_backslash` function L358-360 — `def test_node_with_backslash(g)`
- pub `test_edge_with_reserved_word_rel_type` function L363-367 — `def test_edge_with_reserved_word_rel_type(g)`
- pub `test_edge_with_special_char_rel_type` function L370-374 — `def test_edge_with_special_char_rel_type(g)`
- pub `test_upsert_nodes_batch` function L381-391 — `def test_upsert_nodes_batch(g)`
- pub `test_upsert_edges_batch` function L394-407 — `def test_upsert_edges_batch(g)`
- pub `test_insert_nodes_bulk` function L414-433 — `def test_insert_nodes_bulk(g)` — Test bulk node insertion returns ID mapping.
- pub `test_insert_nodes_bulk_empty` function L436-440 — `def test_insert_nodes_bulk_empty(g)` — Test bulk insert with empty list.
- pub `test_insert_edges_bulk` function L443-463 — `def test_insert_edges_bulk(g)` — Test bulk edge insertion using ID map.
- pub `test_insert_edges_bulk_empty` function L466-469 — `def test_insert_edges_bulk_empty(g)` — Test bulk edge insert with empty list.
- pub `test_insert_edges_bulk_without_id_map` function L472-484 — `def test_insert_edges_bulk_without_id_map(g)` — Test bulk edge insertion without ID map (falls back to lookup).
- pub `test_insert_graph_bulk` function L487-511 — `def test_insert_graph_bulk(g)` — Test combined node and edge bulk insertion.
- pub `test_resolve_node_ids` function L514-525 — `def test_resolve_node_ids(g)` — Test resolving external IDs to internal rowids.
- pub `test_resolve_node_ids_empty` function L528-531 — `def test_resolve_node_ids_empty(g)` — Test resolving empty list.
- pub `test_bulk_insert_mixed_sources` function L534-553 — `def test_bulk_insert_mixed_sources(g)` — Test bulk edge insert connecting new nodes to existing nodes.
- pub `test_bulk_insert_with_typed_properties` function L556-569 — `def test_bulk_insert_with_typed_properties(g)` — Test bulk insert correctly handles different property types.
- pub `test_bulk_insert_performance` function L572-599 — `def test_bulk_insert_performance(g)` — Test that bulk insert is reasonably fast.
- pub `test_pagerank` function L606-623 — `def test_pagerank(g)`
- pub `test_community_detection` function L626-640 — `def test_community_detection(g)`
- pub `test_shortest_path` function L643-655 — `def test_shortest_path(g)`
- pub `test_shortest_path_no_path` function L658-668 — `def test_shortest_path_no_path(g)`
- pub `test_shortest_path_same_node` function L671-677 — `def test_shortest_path_same_node(g)`
- pub `test_degree_centrality` function L680-717 — `def test_degree_centrality(g)`
- pub `test_weakly_connected_components` function L724-761 — `def test_weakly_connected_components(g)`
- pub `test_weakly_connected_components_single_node` function L764-770 — `def test_weakly_connected_components_single_node(g)`
- pub `test_weakly_connected_components_empty_graph` function L773-775 — `def test_weakly_connected_components_empty_graph(g)`
- pub `test_connected_components_alias` function L778-789 — `def test_connected_components_alias(g)` — Test that connected_components is an alias for weakly_connected_components.
- pub `test_strongly_connected_components_with_cycle` function L792-813 — `def test_strongly_connected_components_with_cycle(g)`
- pub `test_strongly_connected_components_no_cycle` function L816-829 — `def test_strongly_connected_components_no_cycle(g)`
- pub `test_strongly_connected_components_empty_graph` function L832-834 — `def test_strongly_connected_components_empty_graph(g)`
- pub `test_strongly_connected_components_mixed` function L837-864 — `def test_strongly_connected_components_mixed(g)`
- pub `test_to_rustworkx` function L871-887 — `def test_to_rustworkx(g)`
- pub `test_to_rustworkx_empty_graph` function L890-898 — `def test_to_rustworkx_empty_graph(g)`
- pub `test_to_rustworkx_preserves_properties` function L901-912 — `def test_to_rustworkx_preserves_properties(g)`
- pub `test_leiden_communities` function L919-951 — `def test_leiden_communities(g)`
- pub `test_leiden_communities_empty_graph` function L954-960 — `def test_leiden_communities_empty_graph(g)`
- pub `test_leiden_communities_with_resolution` function L963-977 — `def test_leiden_communities_with_resolution(g)`
- pub `test_graph_loaded_initially_false` function L984-986 — `def test_graph_loaded_initially_false(g)` — Cache should not be loaded initially.
- pub `test_load_graph` function L989-1000 — `def test_load_graph(g)` — Test loading graph into cache.
- pub `test_load_graph_already_loaded` function L1003-1010 — `def test_load_graph_already_loaded(g)` — Loading when already loaded should return already_loaded status.
- pub `test_unload_graph` function L1013-1022 — `def test_unload_graph(g)` — Test unloading graph cache.
- pub `test_unload_graph_not_loaded` function L1025-1029 — `def test_unload_graph_not_loaded(g)` — Unloading when not loaded should return not_loaded status.
- pub `test_reload_graph` function L1032-1044 — `def test_reload_graph(g)` — Test reloading graph cache after modifications.
- pub `test_reload_graph_not_loaded` function L1047-1055 — `def test_reload_graph_not_loaded(g)` — Reloading when not loaded should load and return reloaded status.
- pub `test_cache_with_pagerank` function L1058-1073 — `def test_cache_with_pagerank(g)` — Test that cached graph works with algorithms.
- pub `test_cache_empty_graph` function L1076-1083 — `def test_cache_empty_graph(g)` — Test caching an empty graph.
- pub `test_graph_query_with_params` function L1091-1099 — `def test_graph_query_with_params(g)` — Test Graph.query() with parameters.
- pub `test_graph_query_with_integer_param` function L1102-1111 — `def test_graph_query_with_integer_param(g)` — Test Graph.query() with integer parameter.
- pub `test_graph_query_with_params_empty_dict` function L1114-1118 — `def test_graph_query_with_params_empty_dict(g)` — Test Graph.query() with empty params dict.
- pub `test_graph_query_without_params_unchanged` function L1121-1125 — `def test_graph_query_without_params_unchanged(g)` — Test Graph.query() backward compatibility without params.
- pub `test_graph_query_params_injection_safe` function L1128-1139 — `def test_graph_query_params_injection_safe(g)` — Test that parameter binding prevents SQL injection.
- pub `test_create_with_map_property` function L1147-1155 — `def test_create_with_map_property(g)` — Test CREATE with a nested map literal stored as JSON.
- pub `test_create_with_list_property` function L1158-1165 — `def test_create_with_list_property(g)` — Test CREATE with a list literal stored as JSON.
- pub `test_nested_dot_access` function L1168-1173 — `def test_nested_dot_access(g)` — Test nested dot access returns correct nested value.
- pub `test_nested_dot_access_deep` function L1176-1181 — `def test_nested_dot_access_deep(g)` — Test deeply nested dot access.
- pub `test_bracket_subscript_access` function L1184-1188 — `def test_bracket_subscript_access(g)` — Test bracket subscript notation for property access.
- pub `test_set_json_map_property` function L1191-1199 — `def test_set_json_map_property(g)` — Test SET with a map value on a property.
- pub `test_set_json_list_property` function L1202-1210 — `def test_set_json_list_property(g)` — Test SET with a list value on a property.
- pub `test_bulk_set_replace` function L1213-1221 — `def test_bulk_set_replace(g)` — Test SET n = {map} replaces all properties.
- pub `test_bulk_set_merge` function L1224-1233 — `def test_bulk_set_merge(g)` — Test SET n += {map} merges properties.
- pub `test_bulk_set_merge_updates_existing` function L1236-1242 — `def test_bulk_set_merge_updates_existing(g)` — Test SET n += {map} updates existing properties.
- pub `test_bulk_set_preserves_labels` function L1245-1251 — `def test_bulk_set_preserves_labels(g)` — Test that bulk SET does not remove labels.
- pub `test_bulk_set_empty_map_clears` function L1254-1262 — `def test_bulk_set_empty_map_clears(g)` — Test SET n = {} clears all properties.
- pub `test_bulk_set_edge` function L1265-1272 — `def test_bulk_set_edge(g)` — Test SET r = {map} on relationships.
- pub `test_mixed_property_and_bulk_set` function L1275-1282 — `def test_mixed_property_and_bulk_set(g)` — Test combining individual property SET with bulk SET.
- pub `test_set_return_single_property` function L1288-1293 — `def test_set_return_single_property(g)` — Test SET + RETURN in a single query.
- pub `test_set_return_bulk_merge` function L1296-1303 — `def test_set_return_bulk_merge(g)` — Test SET n += {map} + RETURN in a single query.
- pub `test_set_return_with_params` function L1306-1315 — `def test_set_return_with_params(g)` — Test parameterized SET + RETURN.
- pub `test_remove_return` function L1318-1324 — `def test_remove_return(g)` — Test REMOVE + RETURN in a single query.

#### bindings/python/tests/test_manager.py

- pub `TestGraphManagerBasic` class L17-124 — `{ test_create_manager, test_list_empty, test_create_graph, test_create_duplicate...` — Basic GraphManager operations.
- pub `test_create_manager` method L20-24 — `def test_create_manager(self, temp_dir)` — Test creating a GraphManager.
- pub `test_list_empty` method L26-29 — `def test_list_empty(self, temp_dir)` — Test listing graphs in empty directory.
- pub `test_create_graph` method L31-38 — `def test_create_graph(self, temp_dir)` — Test creating a new graph.
- pub `test_create_duplicate_fails` method L40-45 — `def test_create_duplicate_fails(self, temp_dir)` — Test that creating duplicate graph raises error.
- pub `test_open_graph` method L47-54 — `def test_open_graph(self, temp_dir)` — Test opening an existing graph.
- pub `test_open_missing_fails` method L56-62 — `def test_open_missing_fails(self, temp_dir)` — Test that opening missing graph raises error.
- pub `test_open_or_create_new` method L64-69 — `def test_open_or_create_new(self, temp_dir)` — Test open_or_create creates new graph.
- pub `test_open_or_create_existing` method L71-76 — `def test_open_or_create_existing(self, temp_dir)` — Test open_or_create opens existing graph.
- pub `test_drop_graph` method L78-85 — `def test_drop_graph(self, temp_dir)` — Test dropping a graph.
- pub `test_drop_missing_fails` method L87-91 — `def test_drop_missing_fails(self, temp_dir)` — Test that dropping missing graph raises error.
- pub `test_list_multiple` method L93-100 — `def test_list_multiple(self, temp_dir)` — Test listing multiple graphs.
- pub `test_contains` method L102-107 — `def test_contains(self, temp_dir)` — Test 'in' operator.
- pub `test_len` method L109-116 — `def test_len(self, temp_dir)` — Test len() on manager.
- pub `test_iter` method L118-124 — `def test_iter(self, temp_dir)` — Test iterating over manager.
- pub `TestGraphManagerData` class L127-156 — `{ test_graph_isolation, test_cached_open }` — Test data operations across graphs.
- pub `test_graph_isolation` method L130-148 — `def test_graph_isolation(self, temp_dir)` — Test that graphs are isolated from each other.
- pub `test_cached_open` method L150-156 — `def test_cached_open(self, temp_dir)` — Test that open returns cached graph.
- pub `TestCrossGraphQueries` class L159-210 — `{ test_query_single_graph, test_query_with_graph_function, test_query_missing_gr...` — Test cross-graph query functionality.
- pub `test_query_single_graph` method L162-175 — `def test_query_single_graph(self, temp_dir)` — Test cross-graph query on single graph.
- pub `test_query_with_graph_function` method L177-189 — `def test_query_with_graph_function(self, temp_dir)` — Test graph() function in cross-graph query.
- pub `test_query_missing_graph_fails` method L191-198 — `def test_query_missing_graph_fails(self, temp_dir)` — Test that querying missing graph raises error.
- pub `test_query_sql` method L200-210 — `def test_query_sql(self, temp_dir)` — Test raw SQL cross-graph query.
- pub `TestGraphManagerPersistence` class L213-237 — `{ test_data_persists, test_graphs_persist }` — Test persistence across manager instances.
- pub `test_data_persists` method L216-228 — `def test_data_persists(self, temp_dir)` — Test that data persists across manager instances.
- pub `test_graphs_persist` method L230-237 — `def test_graphs_persist(self, temp_dir)` — Test that graph list persists.

### bindings/rust

> *Semantic summary to be generated by AI agent.*

#### bindings/rust/build.rs

-  `main` function L7-13 — `()` — When the `bundled-extension` feature is enabled, pre-built extension binaries

### bindings/rust/src/algorithms

> *Semantic summary to be generated by AI agent.*

#### bindings/rust/src/algorithms/centrality.rs

- pub `pagerank` function L18-34 — `(&self, damping: f64, iterations: i32) -> Result<Vec<PageRankResult>>` — Run PageRank algorithm.
- pub `degree_centrality` function L37-54 — `(&self) -> Result<Vec<DegreeCentralityResult>>` — Calculate degree centrality for all nodes.
- pub `betweenness_centrality` function L57-72 — `(&self) -> Result<Vec<BetweennessCentralityResult>>` — Calculate betweenness centrality for all nodes.
- pub `closeness_centrality` function L75-90 — `(&self) -> Result<Vec<ClosenessCentralityResult>>` — Calculate closeness centrality for all nodes.
- pub `eigenvector_centrality` function L97-113 — `(&self, iterations: i32) -> Result<Vec<EigenvectorCentralityResult>>` — Calculate eigenvector centrality for all nodes.
-  `Graph` type L11-114 — `= Graph` — Centrality algorithm implementations.

#### bindings/rust/src/algorithms/community.rs

- pub `community_detection` function L16-32 — `(&self, iterations: i32) -> Result<Vec<CommunityResult>>` — Run community detection using label propagation.
- pub `louvain` function L39-55 — `(&self, resolution: f64) -> Result<Vec<CommunityResult>>` — Run Louvain community detection algorithm.
-  `Graph` type L10-56 — `= Graph` — Community detection algorithm implementations.

#### bindings/rust/src/algorithms/components.rs

- pub `wcc` function L14-29 — `(&self) -> Result<Vec<ComponentResult>>` — Find weakly connected components in the graph.
- pub `scc` function L35-50 — `(&self) -> Result<Vec<ComponentResult>>` — Find strongly connected components in the graph.
-  `Graph` type L10-51 — `= Graph` — Connected components algorithm implementations.

#### bindings/rust/src/algorithms/mod.rs

- pub `PageRankResult` struct L15-22 — `{ node_id: String, user_id: Option<String>, score: f64 }` — PageRank result for a single node.
- pub `CommunityResult` struct L26-33 — `{ node_id: String, user_id: Option<String>, community: i64 }` — Community detection result for a single node.
- pub `ShortestPathResult` struct L37-44 — `{ path: Vec<String>, distance: Option<f64>, found: bool }` — Shortest path result from Dijkstra's algorithm.
- pub `DegreeCentralityResult` struct L48-59 — `{ node_id: String, user_id: Option<String>, in_degree: i64, out_degree: i64, deg...` — Degree centrality result for a single node.
- pub `ComponentResult` struct L63-70 — `{ node_id: String, user_id: Option<String>, component: i64 }` — Connected component result for a single node.
- pub `BetweennessCentralityResult` struct L74-81 — `{ node_id: String, user_id: Option<String>, score: f64 }` — Betweenness centrality result for a single node.
- pub `ClosenessCentralityResult` struct L85-92 — `{ node_id: String, user_id: Option<String>, score: f64 }` — Closeness centrality result for a single node.
- pub `TriangleCountResult` struct L96-105 — `{ node_id: String, user_id: Option<String>, triangles: i64, clustering_coefficie...` — Triangle count result for a single node.
- pub `AStarResult` struct L109-118 — `{ path: Vec<String>, distance: Option<f64>, found: bool, nodes_explored: i64 }` — A* shortest path result.
- pub `TraversalResult` struct L122-129 — `{ user_id: String, depth: i64, order: i64 }` — Traversal result for BFS/DFS.
- pub `NodeSimilarityResult` struct L133-140 — `{ node1: String, node2: String, similarity: f64 }` — Node similarity result using Jaccard coefficient.
- pub `KnnResult` struct L144-151 — `{ neighbor: String, similarity: f64, rank: i64 }` — K-nearest neighbor result.
- pub `EigenvectorCentralityResult` struct L155-162 — `{ node_id: String, user_id: Option<String>, score: f64 }` — Eigenvector centrality result for a single node.
- pub `ApspResult` struct L166-173 — `{ source: String, target: String, distance: f64 }` — All pairs shortest path result for a single pair.
-  `centrality` module L3 — `-` — Graph algorithm implementations and result types.
-  `community` module L4 — `-` — Graph algorithm implementations and result types.
-  `components` module L5 — `-` — Graph algorithm implementations and result types.
-  `parsing` module L6 — `-` — Graph algorithm implementations and result types.
-  `paths` module L7 — `-` — Graph algorithm implementations and result types.
-  `similarity` module L8 — `-` — Graph algorithm implementations and result types.
-  `traversal` module L9 — `-` — Graph algorithm implementations and result types.

#### bindings/rust/src/algorithms/parsing.rs

-  `extract_algo_array` function L13-41 — `(result: &[&Row]) -> Vec<Row>` — Extract a wrapped array result from graph algorithms.
-  `extract_node_id` function L44-50 — `(row: &Row) -> Option<String>` — Extract node_id from a result row.
-  `extract_user_id` function L53-59 — `(row: &Row) -> Option<String>` — Extract user_id from a result row.
-  `extract_float` function L62-70 — `(row: &Row, field: &str) -> f64` — Extract a float score from a result row.
-  `extract_int` function L73-81 — `(row: &Row, field: &str) -> i64` — Extract an integer value from a result row.
-  `extract_string` function L84-90 — `(row: &Row, field: &str) -> Option<String>` — Extract a string value from a result row.

#### bindings/rust/src/algorithms/paths.rs

- pub `shortest_path` function L17-107 — `( &self, source_id: &str, target_id: &str, weight_property: Option<&str>, ) -> R...` — Find the shortest path between two nodes using Dijkstra's algorithm.
- pub `astar` function L117-183 — `( &self, source_id: &str, target_id: &str, lat_prop: Option<&str>, lon_prop: Opt...` — Find shortest path using A* algorithm with heuristic guidance.
- pub `apsp` function L188-211 — `(&self) -> Result<Vec<ApspResult>>` — Compute shortest paths between all pairs of nodes.
-  `Graph` type L9-212 — `= Graph` — Path finding algorithm implementations.

#### bindings/rust/src/algorithms/similarity.rs

- pub `node_similarity` function L18-58 — `( &self, node1_id: Option<&str>, node2_id: Option<&str>, threshold: f64, top_k: ...` — Compute node similarity using Jaccard coefficient.
- pub `knn` function L66-81 — `(&self, node_id: &str, k: i32) -> Result<Vec<KnnResult>>` — Find K-nearest neighbors using Jaccard similarity.
- pub `triangle_count` function L84-99 — `(&self) -> Result<Vec<TriangleCountResult>>` — Count triangles each node participates in.
-  `Graph` type L9-100 — `= Graph` — Similarity algorithm implementations.

#### bindings/rust/src/algorithms/traversal.rs

- pub `bfs` function L16-37 — `(&self, start_id: &str, max_depth: Option<i32>) -> Result<Vec<TraversalResult>>` — Perform breadth-first search traversal from a starting node.
- pub `dfs` function L45-66 — `(&self, start_id: &str, max_depth: Option<i32>) -> Result<Vec<TraversalResult>>` — Perform depth-first search traversal from a starting node.
-  `Graph` type L9-67 — `= Graph` — Graph traversal algorithm implementations.

### bindings/rust/src

> *Semantic summary to be generated by AI agent.*

#### bindings/rust/src/connection.rs

- pub `Connection` struct L14-16 — `{ conn: rusqlite::Connection }` — A GraphQLite database connection.
- pub `open` function L33-36 — `(path: P) -> Result<Self>` — Open a database at the given path.
- pub `open_in_memory` function L48-51 — `() -> Result<Self>` — Open an in-memory database.
- pub `from_rusqlite` function L66-70 — `(conn: rusqlite::Connection) -> Result<Self>` — Create a GraphQLite connection from an existing rusqlite Connection.
- pub `from_rusqlite` function L74-78 — `(conn: rusqlite::Connection) -> Result<Self>` — Create a GraphQLite connection from an existing rusqlite Connection.
- pub `open_with_extension` function L89-96 — `( path: P, extension_path: E, ) -> Result<Self>` — Create a connection with a custom extension path.
- pub `cypher` function L118-133 — `(&self, query: &str) -> Result<CypherResult>` — Execute a Cypher query.
- pub `cypher_with_params` function L160-162 — `(&self, query: &str, params: &serde_json::Value) -> Result<CypherResult>` — Execute a Cypher query with named parameters.
- pub `cypher_builder` function L197-199 — `(&'a self, query: &'a str) -> CypherQuery<'a>` — Create a builder for a parameterized Cypher query.
- pub `execute` function L205-207 — `(&self, sql: &str) -> Result<usize>` — Execute raw SQL.
- pub `sqlite_connection` function L210-212 — `(&self) -> &rusqlite::Connection` — Access the underlying rusqlite connection.
-  `Connection` type L18-213 — `= Connection` — GraphQLite connection wrapper.
-  `execute_cypher_with_params` function L165-181 — `(&self, query: &str, params: &serde_json::Value) -> Result<CypherResult>` — Internal: execute a parameterized Cypher query.
-  `find_extension` function L217-258 — `() -> Result<PathBuf>` — Find the GraphQLite extension library.
-  `load_extension` function L262-281 — `(conn: &rusqlite::Connection, path: &std::path::Path) -> Result<()>` — Load the GraphQLite extension into a connection.
-  `tests` module L284-324 — `-` — GraphQLite connection wrapper.
-  `get_test_extension_path` function L288-305 — `() -> Option<std::path::PathBuf>` — GraphQLite connection wrapper.
-  `test_find_extension` function L309-315 — `()` — GraphQLite connection wrapper.
-  `test_bundled_connection` function L319-323 — `()` — GraphQLite connection wrapper.

#### bindings/rust/src/error.rs

- pub `Error` enum L7-49 — `Sqlite | Json | Cypher | ExtensionNotFound | TypeError | ColumnNotFound | GraphE...` — Error type for GraphQLite operations.

#### bindings/rust/src/lib.rs

- pub `Result` type L80 — `= std::result::Result<T, Error>` — Result type for GraphQLite operations.
-  `algorithms` module L52 — `-` — This crate provides Rust bindings for GraphQLite, allowing you to use
-  `connection` module L53 — `-` — ```
-  `error` module L54 — `-` — ```
-  `graph` module L55 — `-` — ```
-  `manager` module L56 — `-` — ```
-  `platform` module L58 — `-` — ```
-  `query_builder` module L59 — `-` — ```
-  `result` module L60 — `-` — ```
-  `utils` module L61 — `-` — ```

#### bindings/rust/src/manager.rs

- pub `GraphManager` struct L40-44 — `{ base_path: PathBuf, open_graphs: HashMap<String, Graph>, coordinator: Option<C...` — Manager for multiple graph databases in a directory.
- pub `open` function L52-63 — `(base_path: P) -> Result<Self>` — Open a GraphManager for a directory.
- pub `list` function L84-101 — `(&self) -> Result<Vec<String>>` — List all available graphs in the base directory.
- pub `exists` function L108-110 — `(&self, name: &str) -> bool` — Check if a graph exists.
- pub `create` function L125-134 — `(&mut self, name: &str) -> Result<&Graph>` — Create a new graph.
- pub `open_graph` function L149-167 — `(&mut self, name: &str) -> Result<&Graph>` — Open an existing graph.
- pub `open_or_create` function L174-180 — `(&mut self, name: &str) -> Result<&Graph>` — Open a graph, creating it if it doesn't exist.
- pub `get_mut` function L191-193 — `(&mut self, name: &str) -> Option<&mut Graph>` — Get a mutable reference to an open graph.
- pub `drop` function L204-226 — `(&mut self, name: &str) -> Result<()>` — Delete a graph and its database file.
- pub `query` function L241-290 — `(&mut self, cypher: &str, graph_names: &[&str]) -> Result<CypherResult>` — Execute a cross-graph Cypher query.
- pub `query_sql` function L300-350 — `(&mut self, sql: &str, graph_names: &[&str]) -> Result<Vec<Vec<rusqlite::types::...` — Execute a raw SQL query across attached graphs.
- pub `len` function L353-355 — `(&self) -> Result<usize>` — Get the number of graphs in the directory.
- pub `is_empty` function L358-360 — `(&self) -> Result<bool>` — Check if the directory is empty (no graphs).
- pub `contains` function L363-365 — `(&self, name: &str) -> bool` — Check if a graph name is in the directory.
- pub `iter` function L368-370 — `(&self) -> Result<impl Iterator<Item = String>>` — Iterate over graph names.
- pub `graphs` function L382-384 — `(base_path: P) -> Result<GraphManager>` — Create a new GraphManager instance (convenience function).
-  `GraphManager` type L46-371 — `= GraphManager` — in a directory, with cross-graph query support via ATTACH.
-  `graph_path` function L66-68 — `(&self, name: &str) -> PathBuf` — Get the file path for a graph.
-  `ensure_coordinator` function L71-77 — `(&mut self) -> Result<&Connection>` — Get or create the coordinator connection for cross-graph queries.
-  `GraphManager` type L373-379 — `impl Drop for GraphManager` — in a directory, with cross-graph query support via ATTACH.
-  `drop` function L374-378 — `(&mut self)` — in a directory, with cross-graph query support via ATTACH.
-  `tests` module L387-449 — `-` — in a directory, with cross-graph query support via ATTACH.
-  `test_create_manager` function L392-396 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_list_empty` function L399-403 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_create_graph` function L406-412 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_create_duplicate_fails` function L415-420 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_open_missing_fails` function L423-427 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_drop_graph` function L430-437 — `()` — in a directory, with cross-graph query support via ATTACH.
-  `test_list_multiple` function L440-448 — `()` — in a directory, with cross-graph query support via ATTACH.

#### bindings/rust/src/platform.rs

- pub `get_extension_path` function L49-61 — `() -> Result<PathBuf>` — Get the path to the extracted extension binary.
- pub `load_bundled_extension` function L108-129 — `(conn: &rusqlite::Connection) -> Result<()>` — Load the bundled extension into a rusqlite connection.
-  `EXTENSION_FILENAME` variable L14 — `: &str` — Extension filename for current platform
-  `EXTENSION_FILENAME` variable L17 — `: &str` — are embedded in the Rust binary and extracted to a temp file at runtime.
-  `EXTENSION_FILENAME` variable L20 — `: &str` — are embedded in the Rust binary and extracted to a temp file at runtime.
-  `EXTENSION_BYTES` variable L24 — `: &[u8]` — Embedded extension binary for macOS x86_64
-  `EXTENSION_BYTES` variable L28 — `: &[u8]` — Embedded extension binary for macOS ARM64
-  `EXTENSION_BYTES` variable L32 — `: &[u8]` — Embedded extension binary for Linux x86_64
-  `EXTENSION_BYTES` variable L36 — `: &[u8]` — Embedded extension binary for Linux ARM64
-  `EXTENSION_BYTES` variable L40 — `: &[u8]` — Embedded extension binary for Windows x86_64
-  `EXTENSION_PATH` variable L43 — `: Mutex<Option<PathBuf>>` — Cache for the extracted extension path
-  `extract_extension` function L64-105 — `() -> Result<PathBuf>` — Extract the embedded extension binary to a temp file.

#### bindings/rust/src/query_builder.rs

- pub `CypherQuery` struct L29-33 — `{ conn: &'a Connection, query: &'a str, params: serde_json::Map<String, serde_js...` — A builder for constructing and executing parameterized Cypher queries.
- pub `param` function L60-63 — `(mut self, key: &str, value: impl Into<serde_json::Value>) -> Self` — Set an individual named parameter.
- pub `params` function L81-88 — `(mut self, params: &serde_json::Value) -> Self` — Merge parameters from a JSON object.
- pub `run` function L94-101 — `(self) -> Result<CypherResult>` — Execute the query and return results.
-  `new` function L37-43 — `(conn: &'a Connection, query: &'a str) -> Self` — Create a new builder (called internally by `Connection::cypher_builder`).

#### bindings/rust/src/result.rs

- pub `Value` enum L48-63 — `Null | Bool | Integer | Float | String | Array | Object` — A dynamically-typed value returned from a Cypher query.
- pub `from_json` function L69-90 — `(json: JsonValue) -> Self` — Convert from a `serde_json::Value`.
- pub `is_null` function L93-95 — `(&self) -> bool` — Returns `true` if this value is [`Value::Null`].
- pub `as_bool` function L100-105 — `(&self) -> Option<bool>` — Returns the boolean value if this is a [`Value::Bool`].
- pub `as_i64` function L111-116 — `(&self) -> Option<i64>` — Returns the integer value if this is a [`Value::Integer`].
- pub `as_f64` function L121-127 — `(&self) -> Option<f64>` — Returns the float value if this is a [`Value::Float`] or [`Value::Integer`].
- pub `as_str` function L132-137 — `(&self) -> Option<&str>` — Returns a string slice if this is a [`Value::String`].
- pub `get` function L150-155 — `(&self, key: &str) -> Option<&Value>` — Access a field of an [`Value::Object`] by key.
- pub `get_index` function L160-165 — `(&self, index: usize) -> Option<&Value>` — Access an element of a [`Value::Array`] by index.
- pub `as_array` function L168-173 — `(&self) -> Option<&Vec<Value>>` — Returns the array if this is a [`Value::Array`].
- pub `as_object` function L176-181 — `(&self) -> Option<&HashMap<String, Value>>` — Returns the object map if this is a [`Value::Object`].
- pub `Row` struct L212-215 — `{ columns: Vec<String>, values: HashMap<String, Value> }` — A single row from a Cypher query result.
- pub `get_value` function L240-242 — `(&self, column: &str) -> Option<&Value>` — Get a raw [`Value`] by column name.
- pub `get` function L261-266 — `(&self, column: &str) -> crate::Result<T>` — Get a typed value by column name.
- pub `columns` function L269-271 — `(&self) -> &[String]` — Get the column names in this row.
- pub `contains` function L274-276 — `(&self, column: &str) -> bool` — Check if the row contains a column with the given name.
- pub `FromValue` interface L292-295 — `{ fn from_value() }` — Trait for converting from [`Value`] to typed Rust values.
- pub `CypherResult` struct L405-408 — `{ rows: Vec<Row>, columns: Vec<String> }` — Result of a Cypher query, containing zero or more rows.
- pub `empty` function L412-417 — `() -> Self` — Create an empty result with no rows or columns.
- pub `from_json` function L422-497 — `(json_str: &str) -> crate::Result<Self>` — Parse a JSON string into a `CypherResult`.
- pub `len` function L500-502 — `(&self) -> usize` — Returns the number of rows in the result.
- pub `is_empty` function L505-507 — `(&self) -> bool` — Returns `true` if the result contains no rows.
- pub `columns` function L512-514 — `(&self) -> &[String]` — Returns the column names from the query.
- pub `get` function L517-519 — `(&self, index: usize) -> Option<&Row>` — Returns a reference to the row at the given index, or `None` if out of bounds.
- pub `iter` function L522-524 — `(&self) -> impl Iterator<Item = &Row>` — Returns an iterator over the rows.
-  `Value` type L65-182 — `= Value` — ```
-  `Value` type L184-195 — `= Value` — ```
-  `Output` type L185 — `= Value` — ```
-  `index` function L192-194 — `(&self, key: &str) -> &Self::Output` — Index into an Object value by key.
-  `Row` type L217-277 — `= Row` — ```
-  `from_json_object` function L219-226 — `(obj: serde_json::Map<String, JsonValue>) -> Self` — Create a new row from column names and a JSON object.
-  `from_map` function L231-234 — `(map: HashMap<String, Value>) -> Self` — Create a new row from a HashMap of Values.
-  `String` type L297-308 — `impl FromValue for String` — ```
-  `from_value` function L298-307 — `(value: &Value) -> crate::Result<Self>` — ```
-  `from_value` function L311-324 — `(value: &Value) -> crate::Result<Self>` — ```
-  `from_value` function L328-336 — `(value: &Value) -> crate::Result<Self>` — ```
-  `from_value` function L340-349 — `(value: &Value) -> crate::Result<Self>` — ```
-  `from_value` function L353-364 — `(value: &Value) -> crate::Result<Self>` — ```
-  `from_value` function L368-373 — `(value: &Value) -> crate::Result<Self>` — ```
-  `CypherResult` type L410-525 — `= CypherResult` — ```
-  `Item` type L528 — `= &'a Row` — ```
-  `IntoIter` type L529 — `= std::slice::Iter<'a, Row>` — ```
-  `into_iter` function L531-533 — `(self) -> Self::IntoIter` — ```
-  `CypherResult` type L536-543 — `impl IntoIterator for CypherResult` — ```
-  `Item` type L537 — `= Row` — ```
-  `IntoIter` type L538 — `= std::vec::IntoIter<Row>` — ```
-  `into_iter` function L540-542 — `(self) -> Self::IntoIter` — ```
-  `CypherResult` type L545-551 — `= CypherResult` — ```
-  `Output` type L546 — `= Row` — ```
-  `index` function L548-550 — `(&self, index: usize) -> &Self::Output` — ```

#### bindings/rust/src/utils.rs

- pub `CYPHER_RESERVED` variable L6-20 — `: &[&str]` — Cypher reserved keywords that can't be used as relationship types.
- pub `escape_string` function L25-30 — `(s: &str) -> String` — Escape a string for use in Cypher queries.
- pub `sanitize_rel_type` function L35-53 — `(rel_type: &str) -> String` — Sanitize a relationship type for use in Cypher.
- pub `rel_type_pattern` function L58-63 — `(rel_type: Option<&str>) -> String` — Build a Cypher relationship type pattern fragment.
- pub `format_value` function L76-92 — `(v: &str) -> String` — Format a value for inclusion in a Cypher query.
- pub `PropertyValue` enum L120-129 — `Text | Integer | Float | Bool` — A typed property value for graph nodes and edges.
- pub `to_cypher` function L133-144 — `(&self) -> String` — Format this value for embedding in a Cypher query string.
-  `has_leading_zero` function L70-73 — `(s: &str) -> bool` — Check if a string has a leading zero that would be lost in numeric parsing.
-  `PropertyValue` type L131-145 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `PropertyValue` type L147-169 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L152-168 — `(s: &str) -> Self` — Auto-detect type from string (backward-compatible behavior).
-  `PropertyValue` type L171-175 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L172-174 — `(s: String) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L177-181 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L178-180 — `(s: &String) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L183-187 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L184-186 — `(v: i64) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L189-193 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L190-192 — `(v: i32) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L195-199 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L196-198 — `(v: f64) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L201-205 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L202-204 — `(v: f32) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L207-211 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L208-210 — `(v: bool) -> Self` — Utility functions for Cypher query construction.
-  `PropertyValue` type L213-217 — `= PropertyValue` — Utility functions for Cypher query construction.
-  `from` function L214-216 — `(v: usize) -> Self` — Utility functions for Cypher query construction.

### bindings/rust/src/graph

> *Semantic summary to be generated by AI agent.*

#### bindings/rust/src/graph/batch.rs

- pub `upsert_nodes_batch` function L33-46 — `(&self, nodes: I) -> Result<()>` — Batch upsert multiple nodes.
- pub `upsert_edges_batch` function L73-87 — `(&self, edges: I) -> Result<()>` — Batch upsert multiple edges.
-  `Graph` type L10-88 — `= Graph` — For high-performance atomic batch inserts, use the bulk insert methods instead.

#### bindings/rust/src/graph/bulk.rs

- pub `BulkInsertResult` struct L42-49 — `{ nodes_inserted: usize, edges_inserted: usize, id_map: HashMap<String, i64> }` — Result of a bulk insert operation.
- pub `insert_nodes_bulk` function L79-177 — `(&self, nodes: I) -> Result<HashMap<String, i64>>` — Insert multiple nodes in a single transaction with minimal overhead.
- pub `insert_edges_bulk` function L208-326 — `( &self, edges: I, id_map: &HashMap<String, i64>, ) -> Result<usize>` — Insert multiple edges using pre-resolved internal IDs.
- pub `insert_graph_bulk` function L340-369 — `( &self, nodes: NI, edges: EI, ) -> Result<BulkInsertResult>` — Bulk insert both nodes and edges in a single operation.
- pub `resolve_node_ids` function L384-421 — `(&self, external_ids: I) -> Result<HashMap<String, i64>>` — Resolve multiple external node IDs to internal rowids in a single query.
-  `Graph` type L51-465 — `= Graph` — ```
-  `ensure_property_key` function L424-441 — `(&self, conn: &rusqlite::Connection, key: &str) -> Result<i64>` — ```
-  `lookup_node_id` function L444-464 — `(&self, conn: &rusqlite::Connection, external_id: &str) -> Result<i64>` — ```
-  `tests` module L468-665 — `-` — ```
-  `Props` type L472 — `= Vec<(&'static str, &'static str)>` — ```
-  `empty_props` function L474-476 — `() -> Props` — ```
-  `test_bulk_insert_nodes` function L479-496 — `()` — ```
-  `test_bulk_insert_edges` function L499-526 — `()` — ```
-  `test_bulk_insert_graph` function L529-544 — `()` — ```
-  `test_resolve_node_ids` function L547-560 — `()` — ```
-  `test_bulk_insert_mixed_sources` function L563-589 — `()` — ```
-  `test_bulk_insert_performance` function L592-664 — `()` — ```

#### bindings/rust/src/graph/edges.rs

- pub `has_edge` function L9-23 — `(&self, source_id: &str, target_id: &str, rel_type: Option<&str>) -> Result<bool...` — Check if a directed edge exists from source to target.
- pub `get_edge` function L26-39 — `(&self, source_id: &str, target_id: &str, rel_type: Option<&str>) -> Result<Opti...` — Get edge properties between two nodes.
- pub `upsert_edge` function L46-87 — `( &self, source_id: &str, target_id: &str, props: I, rel_type: &str, ) -> Result...` — Create or update an edge between two nodes.
- pub `delete_edge` function L90-100 — `(&self, source_id: &str, target_id: &str, rel_type: Option<&str>) -> Result<()>` — Delete the directed edge between two nodes.
- pub `get_all_edges` function L103-106 — `(&self) -> Result<CypherResult>` — Get all edges in the graph.
-  `Graph` type L7-107 — `= Graph` — Edge operations for Graph.

#### bindings/rust/src/graph/mod.rs

- pub `GraphStats` struct L20-25 — `{ node_count: i64, edge_count: i64 }` — Graph statistics containing node and edge counts.
- pub `Graph` struct L31-33 — `{ conn: Connection }` — High-level graph operations.
- pub `open` function L41-44 — `(path: P) -> Result<Self>` — Open a graph database.
- pub `open_with_extension` function L55-61 — `( path: P, extension_path: E, ) -> Result<Self>` — Open a graph database with a custom extension path.
- pub `open_in_memory` function L64-67 — `() -> Result<Self>` — Open an in-memory graph database.
- pub `from_connection` function L70-72 — `(conn: Connection) -> Self` — Create a Graph from an existing [`Connection`].
- pub `connection` function L75-77 — `(&self) -> &Connection` — Access the underlying Connection.
- pub `query` function L80-82 — `(&self, cypher: &str) -> Result<CypherResult>` — Execute a raw Cypher query.
- pub `query_with_params` function L91-93 — `(&self, cypher: &str, params: &serde_json::Value) -> Result<CypherResult>` — Execute a raw Cypher query with named parameters.
- pub `query_builder` function L109-111 — `(&'a self, cypher: &'a str) -> CypherQuery<'a>` — Create a builder for a parameterized Cypher query.
- pub `query_params` function L132-142 — `( &self, cypher: &str, params: &[(&str, &serde_json::Value)], ) -> Result<Cypher...` — Execute a parameterized Cypher query in a single call.
- pub `load_graph` function L168-173 — `(&self) -> Result<CacheStatus>` — Load the graph into an in-memory CSR cache for fast algorithm execution.
- pub `unload_graph` function L192-197 — `(&self) -> Result<CacheStatus>` — Free the cached graph from memory.
- pub `reload_graph` function L216-221 — `(&self) -> Result<CacheStatus>` — Reload the graph cache with the latest data.
- pub `graph_loaded` function L240-245 — `(&self) -> Result<bool>` — Check if the graph cache is currently loaded.
- pub `CacheStatus` struct L250-259 — `{ status: String, node_count: Option<i64>, edge_count: Option<i64> }` — Cache operation status returned by load/unload/reload operations.
- pub `graph` function L268-270 — `(path: P) -> Result<Graph>` — Create a new Graph instance (convenience function).
-  `batch` module L5 — `-` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `bulk` module L6 — `-` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `edges` module L7 — `-` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `nodes` module L8 — `-` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `queries` module L9 — `-` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `Graph` type L35-246 — `= Graph` — The `Graph` struct provides an ergonomic interface for common graph operations.
-  `CacheLoadedStatus` struct L263-265 — `{ loaded: bool }` — Response from graph_loaded() query.

#### bindings/rust/src/graph/nodes.rs

- pub `has_node` function L9-20 — `(&self, node_id: &str) -> Result<bool>` — Check if a node with the given ID exists.
- pub `get_node` function L25-35 — `(&self, node_id: &str) -> Result<Option<Value>>` — Get a node by ID.
- pub `upsert_node` function L41-74 — `(&self, node_id: &str, props: I, label: &str) -> Result<()>` — Create or update a node.
- pub `delete_node` function L77-84 — `(&self, node_id: &str) -> Result<()>` — Delete a node and all its relationships.
- pub `get_all_nodes` function L87-100 — `(&self, label: Option<&str>) -> Result<Vec<Value>>` — Get all nodes, optionally filtered by label.
-  `Graph` type L7-101 — `= Graph` — Node operations for Graph.

#### bindings/rust/src/graph/queries.rs

- pub `node_degree` function L9-19 — `(&self, node_id: &str) -> Result<i64>` — Get the degree (number of connections) of a node.
- pub `get_neighbors` function L22-35 — `(&self, node_id: &str) -> Result<Vec<Value>>` — Get all neighboring nodes (connected via any edge direction).
- pub `stats` function L38-55 — `(&self) -> Result<GraphStats>` — Get graph statistics (node and edge counts).
- pub `get_edges_from` function L60-66 — `(&self, node_id: &str) -> Result<CypherResult>` — Get all outgoing edges from a node.
- pub `get_edges_to` function L71-77 — `(&self, node_id: &str) -> Result<CypherResult>` — Get all incoming edges to a node.
- pub `get_edges_by_type` function L82-90 — `(&self, node_id: &str, rel_type: &str) -> Result<CypherResult>` — Get outgoing edges of a specific type from a node.
- pub `get_node_edges` function L95-101 — `(&self, node_id: &str) -> Result<CypherResult>` — Get all edges (both directions) connected to a node.
-  `Graph` type L7-102 — `= Graph` — Query operations for Graph.

### bindings/rust/tests

> *Semantic summary to be generated by AI agent.*

#### bindings/rust/tests/integration.rs

-  `test_connection` function L7-9 — `() -> Connection` — Create a test connection.
-  `test_graph` function L12-14 — `() -> Graph` — Create a test graph.
-  `test_open_memory` function L17-20 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_concurrent_parsing` function L25-47 — `()` — Verifies concurrent parsing: multiple threads each run Cypher queries in parallel.
-  `test_open_file` function L50-58 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_create_node` function L61-75 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_create_relationship` function L78-96 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_return_scalar` function L99-110 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_multiple_rows` function L113-126 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_aggregation` function L129-142 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_empty_result` function L145-152 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_iteration` function L155-169 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_column_not_found` function L172-182 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_type_error` function L185-195 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_optional_values` function L198-215 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_columns` function L218-227 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_algorithms` function L230-250 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_upsert_node` function L257-268 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_upsert_edge` function L271-281 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_stats` function L284-297 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_degree` function L300-314 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_neighbors` function L317-331 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_delete_node` function L334-342 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_delete_edge` function L345-356 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_upsert_edge_multiple_types` function L359-372 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_get_edge_by_type` function L375-409 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_delete_edge_by_type` function L412-426 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_has_edge_by_type` function L429-439 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_upsert_edge_updates_properties` function L442-469 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_upsert_edge_update_empty_props` function L472-494 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_query` function L497-508 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_batch_nodes` function L511-523 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_api_algorithms` function L526-543 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_shortest_path` function L546-572 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_degree_centrality` function L575-609 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_wcc` function L612-648 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_wcc_empty` function L651-656 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_wcc_alias` function L659-672 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_scc_cycle` function L675-694 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_scc_no_cycle` function L697-715 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_scc_empty` function L718-723 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_node_property` function L730-757 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_multiple_properties` function L760-780 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_label` function L783-820 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_edge_property` function L823-852 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_with_where` function L855-890 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_nonexistent_property` function L893-910 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_no_match` function L913-919 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_in_literal_list_match` function L926-934 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_in_literal_list_no_match` function L937-945 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_in_with_where_clause` function L948-961 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_in_with_integers` function L964-977 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_in_empty_result` function L980-989 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_utility_functions` function L992-1002 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_starts_with_match` function L1009-1017 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_starts_with_no_match` function L1020-1027 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_ends_with_match` function L1030-1037 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_ends_with_no_match` function L1040-1047 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_contains_match` function L1050-1057 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_contains_no_match` function L1060-1067 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_operators_in_where` function L1070-1100 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_to_upper` function L1107-1113 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_to_lower` function L1116-1122 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_trim` function L1125-1131 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_ltrim` function L1134-1140 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_rtrim` function L1143-1149 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_substring` function L1152-1164 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_replace` function L1167-1173 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_reverse` function L1176-1182 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_left` function L1185-1191 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_right` function L1194-1200 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_split` function L1203-1215 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_string_functions_with_properties` function L1218-1229 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_abs` function L1236-1249 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_ceil` function L1252-1259 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_floor` function L1262-1269 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_round` function L1272-1283 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_sqrt` function L1286-1302 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_sign` function L1305-1313 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_rand` function L1316-1323 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_math_functions_with_properties` function L1326-1341 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_size` function L1348-1354 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_head` function L1357-1363 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_tail` function L1366-1378 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_last` function L1381-1387 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_range` function L1390-1402 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_list_range_with_step` function L1405-1417 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_union_basic` function L1424-1434 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_union_all` function L1437-1448 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_union_removes_duplicates` function L1451-1459 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_with_basic` function L1466-1480 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_with_aggregation` function L1483-1498 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_with_order_by_limit` function L1501-1515 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_case_simple` function L1522-1537 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_case_generic` function L1540-1554 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_coalesce` function L1561-1573 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_is_null` function L1576-1587 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_is_not_null` function L1590-1601 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_to_string` function L1608-1619 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_to_integer` function L1622-1632 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_to_float` function L1635-1649 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_to_boolean` function L1652-1664 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_exists_property` function L1671-1682 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_create_multiple_labels` function L1689-1704 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_match_multiple_labels` function L1707-1720 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_return_node_all_labels` function L1723-1738 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_optional_match_with_results` function L1745-1756 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_optional_match_no_results` function L1759-1771 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_unwind_basic` function L1778-1788 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_unwind_with_create` function L1791-1803 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_unwind_with_list_literal` function L1806-1817 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_manager` function L1824-1828 — `() -> (GraphManager, tempfile::TempDir)` — Create a test GraphManager.
-  `test_manager_create` function L1831-1836 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_create_graph` function L1839-1853 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_create_duplicate_fails` function L1856-1861 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_open_graph` function L1864-1870 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_open_missing_fails` function L1873-1883 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_open_or_create` function L1886-1901 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_drop_graph` function L1904-1912 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_drop_missing_fails` function L1915-1919 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_list_multiple` function L1922-1931 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_contains` function L1934-1940 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_len` function L1943-1951 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_graph_isolation` function L1954-1989 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_cross_graph_query` function L1992-2011 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_query_missing_graph_fails` function L2014-2022 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_query_sql` function L2025-2041 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graphs_convenience_function` function L2044-2052 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_manager_iter` function L2055-2063 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_regression_gqlite_t_0092_detach_delete_property_filter` function L2070-2096 — `()` — Regression test for GQLITE-T-0092: DETACH DELETE deletes all nodes
-  `test_graph_loaded_initially_false` function L2103-2106 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_load_graph` function L2109-2123 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_load_graph_already_loaded` function L2126-2136 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_unload_graph` function L2139-2151 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_unload_graph_not_loaded` function L2154-2160 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_reload_graph` function L2163-2178 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_reload_graph_not_loaded` function L2181-2192 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_cache_with_pagerank` function L2195-2212 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_cache_empty_graph` function L2215-2224 — `()` — Integration tests for GraphQLite Rust bindings.
-  `BulkProps` type L2231 — `= Vec<(&'static str, &'static str)>` — Integration tests for GraphQLite Rust bindings.
-  `empty_bulk_props` function L2233-2235 — `() -> BulkProps` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_nodes` function L2238-2264 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_nodes_empty` function L2267-2276 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_edges` function L2279-2314 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_edges_empty` function L2317-2333 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_edges_fallback_lookup` function L2336-2354 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_graph` function L2357-2382 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_resolve_node_ids` function L2385-2400 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_resolve_node_ids_empty` function L2403-2408 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_mixed_sources` function L2411-2444 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_with_typed_properties` function L2447-2475 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_insert_verifies_with_graph_api` function L2478-2512 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_string_match` function L2519-2528 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_integer_filter` function L2531-2541 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_in_create` function L2544-2555 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_bulk_params` function L2558-2567 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_mixed_param_and_params` function L2570-2581 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_run_no_params` function L2584-2592 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_injection_safe` function L2595-2605 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_builder_backward_compat` function L2608-2613 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_query_builder` function L2616-2625 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_graph_query_without_params_unchanged` function L2628-2633 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_create_with_map_property` function L2640-2650 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_create_with_list_property` function L2653-2663 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_nested_dot_access` function L2666-2675 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_nested_dot_access_deep` function L2678-2687 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_set_json_map_property` function L2690-2701 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_set_json_list_property` function L2704-2715 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_replace` function L2718-2732 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_merge` function L2735-2749 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_merge_updates_existing` function L2752-2763 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_edge` function L2766-2779 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_empty_map_clears` function L2782-2797 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_mixed_property_and_bulk_set` function L2800-2811 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_bulk_set_with_builder_params` function L2814-2825 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_set_single_property_return` function L2828-2837 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_set_bulk_replace_return` function L2840-2855 — `()` — Integration tests for GraphQLite Rust bindings.
-  `test_remove_property_return` function L2858-2872 — `()` — Integration tests for GraphQLite Rust bindings.

### docs/theme

> *Semantic summary to be generated by AI agent.*

#### docs/theme/version-select.js

-  `getCurrentVersion` function L8-12 — `function getCurrentVersion()`
-  `initVersionSelect` function L15-51 — `function initVersionSelect()`

### examples/llm-graphrag

> *Semantic summary to be generated by AI agent.*

#### examples/llm-graphrag/analyze.py

- pub `analyze_graph` function L15-112 — `def analyze_graph(db_path: str = "hotpotqa.db")` — Analyze the graph structure.
- pub `main` function L115-120 — `def main()`

#### examples/llm-graphrag/hotpotqa.py

- pub `Paragraph` class L23-26 — `-` — A Wikipedia paragraph from HotpotQA.
- pub `SupportingFact` class L30-33 — `-` — A supporting fact for answering a question.
- pub `HotpotExample` class L37-45 — `-` — A single HotpotQA example.
- pub `download_dataset` function L48-97 — `def download_dataset( url: str = HOTPOTQA_DEV_DISTRACTOR, cache_dir: Path | str ...` — Download HotpotQA dataset if not cached.
- pub `load_dataset` function L100-150 — `def load_dataset( path: Path | str, limit: int | None = None, verbose: bool = Tr...` — Load HotpotQA examples from JSON file.
- pub `get_supporting_text` function L153-169 — `def get_supporting_text(example: HotpotExample) -> list[str]` — Get the actual supporting sentences for an example.
- pub `get_unique_titles` function L172-178 — `def get_unique_titles(examples: list[HotpotExample]) -> set[str]` — Get all unique Wikipedia article titles from examples.
- pub `get_dataset_stats` function L181-197 — `def get_dataset_stats(examples: list[HotpotExample]) -> dict` — Get statistics about the dataset.

#### examples/llm-graphrag/ingest.py

- pub `make_safe_id` function L38-41 — `def make_safe_id(s: str) -> str` — Create a safe node ID from a string by removing problematic characters.
- pub `load_vec_extension` function L44-60 — `def load_vec_extension(g)` — Load sqlite-vec extension for vector similarity search.
- pub `serialize_embedding` function L63-65 — `def serialize_embedding(embedding: np.ndarray) -> bytes` — Serialize embedding to bytes for sqlite-vec.
- pub `ingest_hotpotqa` function L68-273 — `def ingest_hotpotqa( db_path: str = "hotpotqa.db", limit: int | None = None, ver...` — Ingest HotpotQA dataset into GraphQLite.
- pub `main` function L276-303 — `def main()`

#### examples/llm-graphrag/ollama_client.py

- pub `Message` class L14-17 — `-` — A chat message.
- pub `OllamaClient` class L20-210 — `{ __init__, chat, chat_stream, generate, is_available, list_models, close }` — Client for Ollama's REST API.
- pub `__init__` method L27-44 — `def __init__( self, model: str = "qwen3:8b", base_url: str = "http://localhost:1...` — Initialize Ollama client.
- pub `chat` method L46-91 — `def chat( self, messages: list[Message], temperature: float = 0.7, stream: bool ...` — Send a chat completion request.
- pub `chat_stream` method L103-147 — `def chat_stream( self, messages: list[Message], temperature: float = 0.7, ) -> G...` — Stream chat completion tokens.
- pub `generate` method L149-182 — `def generate( self, prompt: str, system: str | None = None, temperature: float =...` — Simple generate endpoint (non-chat).
- pub `is_available` method L184-190 — `def is_available(self) -> bool` — Check if Ollama server is available.
- pub `list_models` method L192-200 — `def list_models(self) -> list[str]` — List available models.
- pub `close` method L202-204 — `def close(self)` — Close the HTTP client.
- pub `__enter__` method L206-207 — `def __enter__(self)`
- pub `__exit__` method L209-210 — `def __exit__(self, *args)`
-  `_handle_stream` method L93-101 — `def _handle_stream(self, response: httpx.Response) -> str` — Handle streaming response.

#### examples/llm-graphrag/rag.py

- pub `deserialize_embedding` function L19-22 — `def deserialize_embedding(blob: bytes) -> np.ndarray` — Deserialize embedding from sqlite-vec bytes.
- pub `GraphRAG` class L34-254 — `{ __init__, get_community_articles, embed_query, vector_search, get_related_arti...` — GraphRAG query interface.
- pub `__init__` method L37-48 — `def __init__(self, db_path: str = "hotpotqa.db", model: str = "qwen3:8b")`
- pub `get_community_articles` method L63-89 — `def get_community_articles(self, article_id: str, limit: int = 3) -> tuple[list[...` — Get other articles in the same community.
- pub `embed_query` method L91-94 — `def embed_query(self, query: str) -> bytes` — Embed a query string.
- pub `vector_search` method L96-121 — `def vector_search(self, query: str, k: int = 5) -> list[dict]` — Find top-k similar articles using vector search.
- pub `get_related_articles` method L123-141 — `def get_related_articles(self, article_id: str, max_hops: int = 1) -> tuple[list...` — Get articles related via COOCCURS edges.
- pub `build_context` method L143-199 — `def build_context(self, query: str, k: int = 3, include_related: bool = True) ->...` — Build context for answering a query.
- pub `query` method L201-249 — `def query(self, question: str, use_llm: bool = True) -> dict` — Answer a question using GraphRAG.
- pub `close` method L251-254 — `def close(self)` — Close connections.
- pub `print_section` function L257-262 — `def print_section(title: str, content: str, width: int = 70)` — Print a section with a header.
- pub `main` function L265-377 — `def main()`
-  `_get_communities` method L50-61 — `def _get_communities(self) -> dict[str, int]` — Lazy-load community detection results.

